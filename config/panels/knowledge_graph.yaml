# ──────────────────────────────────────────────────────────────────────────────
# Panel type: knowledge_graph
#
# Static placeholder ASCII node/edge graph (Decision 3 of
# docs/tuber_base_layout_plan.md: STUB structure only — wiring to real
# campaign-pack/Postgres data is explicitly a separate future task). Clearly
# labeled "(placeholder)" in the render.
# ──────────────────────────────────────────────────────────────────────────────
type: knowledge_graph
title: "Knowledge"
border_color: blue
command: "python3 /app/knowledge_graph_pane.py --config {config_path}"
