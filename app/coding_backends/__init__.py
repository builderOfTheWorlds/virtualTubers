# Adapters implementing coding_backend.CodingBackend, one per provider.
# Imported lazily by build_coding_backend() so a worker only pays for (and
# only needs installed) the tool it actually selected.
